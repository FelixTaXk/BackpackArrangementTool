# 超越版求解引擎 · 进度说明文档

> 封存时间：2026-08-14 17:50（用户换设备前封存）
> 本文档用于跨设备接续工作：包含任务快照、关键决策、验收基线、未完成项与恢复指引。
> 完整设计思考（七维超越对比、数据结构、能量公式、分期规划、风险缓解、被否决方案）见同目录《超越版求解引擎设计.md》。

## 一、总体进度：约 45%

| 阶段 | 内容 | 状态 |
|---|---|---|
| 期 0 | 脚手架（engineMode 下拉、persistence 可选字段、script 登记） | ✅ 完成并验收 |
| 期 1 | 核心引擎（SA+ALNS+并行回火+Portfolio+增量评分） | ✅ 完成，QA 门禁 10/10 通过（commit bb05447） |
| 期 2 | LNS + 配对感知上界 | 🔶 实现 100%，自测约 30%，验收约 10%，**暂停中** |
| 期 3 | 对接放量（statusBox SA 行、默认 auto、docs 回归清单、SAB 增强档） | ⬜ 未开始 |
| 期 4 | 《我要当老祖》游戏主题 UX | ⬜ 未开始（5 张游戏实机截图为视觉依据，风格令牌已定稿于设计文档） |

## 二、任务板快照

| ID | 任务 | 状态 |
|---|---|---|
| 23 | 期1：score-shared.js + engine-encoding.js 基础层 | ✅ 完成 |
| 24 | 期1：engine-worker.js SA+ALNS 主体 | ✅ 完成 |
| 25 | 期0+1：orchestrator 与集成脚手架 | ✅ 完成 |
| 26 | 期1：验证与验收指标 | ✅ 完成（复验 10/10，commit bb05447） |
| 27 | 期2：LNS+配对感知上界 | 🔶 实现完成、验收未完（见第四节） |
| 28 | 期2：验证 | ⬜ 待办（被任务 27 阻塞） |
| 29/30 | 期3：对接放量 + 验证 | ⬜ 待办 |
| 31/32 | 期4：游戏主题 UX + 验证 | ⬜ 待办 |

## 三、关键决策与铁律（换设备后必须延续）

1. **旁路原则**：js/solver-worker.js 与 js/ui-result.js 字节级零改动；legacy 档与 fast 档恒走旧 DFS，默认行为与改造前完全一致。
2. **冻结契约**：新引擎输出逐字段对齐旧引擎——progress/incumbent/done 消息字段集、serializeBest 键集、placements 项 21 键、bonusEvents/priorityLinks 结构、solverMeta（仅加法新增 engine 键）。QA 已逐键 diff 确认为空。
3. **正确性架构**：热路径快算、晋级全算——SA 内部标量能量驱动，任何对外 incumbent 必须经 score-shared 全量重算 + 主线程 compareSolverBest 真字典序复核。
4. **RNG 基线**：期 1 修复后算子集 6→7、能量权重与重热策略变化，RNG 序列与最初基线不同（属预期）；当前基线以"同种子两次运行逐字一致"为准（QA 三跑验证通过）。
5. **file:// 兼容**：并行回火默认走主线程 broker（Transferable 消息），SharedArrayBuffer 增强档仅在 crossOriginIsolated 时启用，隐藏开关 `window.__ENGINE_TEMPERING_OFF__` 可整体禁用。

## 四、任务 27 未完成项（恢复后第一件事）

实现已 100% 落盘（js/engine-worker.js：三种摧毁算子 random/worst/region、anytime DFS 修复 500ms 分片、配对感知 UB 剪枝、lnsSmall/lnsBig 注册进 ALNS，另修 pool=1 早退 bug、严格改进判定、UB 缓存移除；node --check 通过）。

**卡点**：满盘僵局验收用例造不出来。随机搜索呈双峰分布——31 种子中 27 个 DFS 30s 也不全装（几何不可装），4 个可全装种子（205/227/236/295）上 SA 关闭 LNS 也全装（32 组扫描无 winners）。结论：僵局与 SA 种子相关而非实例固有。

**恢复步骤**：
1. 人工构造几何陷阱僵局清单（窄缝、孤立单格洞等形状组合）
2. 补做验收：T2 UB 保守性断言、T3 delta 对账（2000 步随机游走 <1e-6）、T4 种子复现、T5 门禁回归（清单 A/B SA 3s 不低于期 1 基线）
3. 清理临时脚本 → 报告 → QA（Chris）独立复验（任务 28）→ git 提交

## 五、期 1 验收基线（期 2 及以后不得退化）

| 指标 | 基线 |
|---|---|
| SA 3s vs DFS 20s（QA 清单口径：A=12件/2手动优先级/面积33，B=16件/1手动优先级/面积41） | 8/8 种子 cmp=1 全胜；A：68767.20（DFS 基准 66290.70，+3.7%）；B：47967~48795（基准 46021，+4.2~6.0%），全部 complete |
| 开发自测清单口径（genItems） | A 15974.96 量级、B ≈14009.64 量级 |
| init 构建（encBuildModel+encBuildBundle，P≈516） | P50=3.0ms / P95=3.9ms（≤5ms） |
| 迭代速率 | ≥19.4 万/秒（门禁 15 万） |
| 回火开销 | <2%（实测噪声范围内） |
| 明细对账 | ΣbonusEvents.bonus === bonusScore，差=0 |

## 六、换设备接续指引

1. 本仓库**无远程仓库**：换设备需整个文件夹（含 .git）拷贝，或先加 remote 推送后 clone。
2. 打开项目后对助手说：「继续超越版求解引擎任务，先读 docs/进度说明.md 与 docs/超越版求解引擎设计.md」即可从第四节断点接续。
3. 用户指令（2026-08-14）：任务 27 完成后暂停，验收等换设备后再继续。

## 七、git 提交历史（相关）

- `bb05447` fix: SA引擎期1验收缺陷修复（收敛/重建守卫/init提速/消息分片）— 门禁 10/10
- `860da3c` feat: 超越版SA+ALNS求解引擎期0+1（集成脚手架）与法宝库两步式改造
- 本次封存提交：期 2 LNS 实现（engine-worker.js）+ 设计文档与进度文档入档
