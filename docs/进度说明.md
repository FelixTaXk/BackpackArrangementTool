# 超越版求解引擎 · 进度说明文档

> 封存时间：2026-08-14 17:50（用户换设备前封存）；2026-08-15 更新：期 2 收口（LNS 默认关闭验收，见第四节）；期 3 对接放量+评审修复；期 4 主题 UX 完成并验收，总体进度 100%（见四点六分节与遗留项）
> 本文档用于跨设备接续工作：包含任务快照、关键决策、验收基线、未完成项与恢复指引。
> 完整设计思考（七维超越对比、数据结构、能量公式、分期规划、风险缓解、被否决方案）见同目录《超越版求解引擎设计.md》。

## 一、总体进度：100%

| 阶段 | 内容 | 状态 |
|---|---|---|
| 期 0 | 脚手架（engineMode 下拉、persistence 可选字段、script 登记） | ✅ 完成并验收 |
| 期 1 | 核心引擎（SA+ALNS+并行回火+Portfolio+增量评分） | ✅ 完成，QA 门禁 10/10 通过（commit bb05447） |
| 期 2 | LNS + 配对感知上界 | ✅ **完成并验收**：实现正确（缺陷已修+独立复验）；LNS **默认关闭**收口（功效无法与 RNG 扰动剥离，开启挤占预算），代码保留待重启（见第四节） |
| 期 3 | 对接放量（statusBox SA 行、默认 auto、docs 回归清单、SAB 增强档） | ✅ **完成并验收**：实施+三评审合并发现修复完成（见第四分节/四点五分节）；浏览器 E2E 验证由期 4 主题 UX 的全页面 E2E 覆盖通过（statusBox SA 实时行/默认 auto/SAB 可用性展示/筛选等逐项 PASS） |
| 期 4 | 《我要当老祖》游戏主题 UX | ✅ **完成并验收**（commit 3d78b06）：浏览器 E2E 全项 PASS，5 张基线截图入档 docs/e2e-t10-01~05（见四点六分节与遗留项） |

## 二、任务板快照

| ID | 任务 | 状态 |
|---|---|---|
| 23 | 期1：score-shared.js + engine-encoding.js 基础层 | ✅ 完成 |
| 24 | 期1：engine-worker.js SA+ALNS 主体 | ✅ 完成 |
| 25 | 期0+1：orchestrator 与集成脚手架 | ✅ 完成 |
| 26 | 期1：验证与验收指标 | ✅ 完成（复验 10/10，commit bb05447） |
| 27 | 期2：LNS+配对感知上界 | ✅ 完成（缺陷修复+复验通过；LNS 默认关闭收口，见第四节） |
| 28 | 期2：验证 | ✅ 完成（QA 复验由 Terry 独立完成：三档对照复验 + 缺陷修复复验，见第四节） |
| 29/30 | 期3：对接放量 + 验证 | 29 ✅ 实施完成（见第四分节）；30 ✅ 完成（浏览器 E2E 由期 4 全页面 E2E 覆盖通过） |
| 31/32 | 期4：游戏主题 UX + 验证 | 31 ✅ 完成（commit 3d78b06）；32 ✅ 完成（浏览器 E2E 全项 PASS，截图入档 e2e-t10-01~05） |

## 三、关键决策与铁律（换设备后必须延续）

1. **旁路原则**：js/solver-worker.js 与 js/ui-result.js 字节级零改动；legacy 档与 fast 档恒走旧 DFS，默认行为与改造前完全一致。
2. **冻结契约**：新引擎输出逐字段对齐旧引擎——progress/incumbent/done 消息字段集、serializeBest 键集、placements 项 21 键、bonusEvents/priorityLinks 结构、solverMeta（仅加法新增 engine 键）。QA 已逐键 diff 确认为空。
3. **正确性架构**：热路径快算、晋级全算——SA 内部标量能量驱动，任何对外 incumbent 必须经 score-shared 全量重算 + 主线程 compareSolverBest 真字典序复核。
4. **RNG 基线**：期 1 修复后算子集 6→7、能量权重与重热策略变化，RNG 序列与最初基线不同（属预期）；当前基线以"同种子两次运行逐字一致"为准（QA 三跑验证通过）。注意：默认关闭档的逐字一致比对对象是 **5cdda6b 实验档 A（LNS 关）**，而非期 1 bb05447——期 1 无 op7/op8，而当前 9 算子轮盘赌中 op7/op8 约 14.6% 份额为空转迭代，该差异由 5cdda6b 引入、e1034dc 刻意保留以对齐档 A。
5. **file:// 兼容**：并行回火默认走主线程 broker（Transferable 消息），SharedArrayBuffer 增强档仅在 crossOriginIsolated 时启用，隐藏开关 `window.__ENGINE_TEMPERING_OFF__` 可整体禁用。
6. **LNS 定位（期 2 收口决策，2026-08-15 用户拍板）**：LNS（op7=lnsSmall/op8=lnsBig）实现正确但僵局救回功效无法与 RNG 扰动剥离（224 次三档对照 winners 7 vs noise 28），且开启时均分 −0.64%（预算挤占）→ **默认关闭、代码保留待重启**。开关链路：orchestrator init meta 新增 `lnsEnabled` 键（缺省/非真值=关闭；隐藏开关 `window.__ENGINE_LNS_ON__` 为真时置 true，冻结契约只加法）；Worker 侧关闭时 ewRunOp 对 op7/op8 短路为 no-op 且**不消耗 RNG**（与三档实验「LNS 关」档 A 逐字一致）。注意：op7/op8 算子权重（初值 0.6/地板 0.3）**有意不清零**——清零会改变 ewPickOp 的 RNG→算子映射（原 no-op 份额转投逃逸重建），破坏与档 A 基线逐字一致；短路先于一切权重生效，地板无法复活 LNS 执行。

## 四、期 2 验收结论（2026-08-15 收口）

### 4.1 结论摘要

期 2 按「实现正确、LNS 默认关闭」验收收口。验收过程发现并修复了 ewLnsSearch 两处致命缺陷与 UB 配对松弛低估，修复经独立复验全部通过；但 224 次三档对照实验表明 LNS 僵局救回功效无法与 RNG 扰动剥离，且开启时均分 −0.64%（预算挤占）。用户拍板：LNS 默认关闭、代码保留待后续重启，直接推进期 3。临时实验目录 scripts/exp/（含三份报告原件与全部脚本/日志/原始数据）已于本次收口删除，本节为结论存档。

### 4.2 三份报告结论要点（原件已随 scripts/exp/ 删除）

- **DEADLOCK-REPORT（僵局用例构造）**：成功构造 9 实例（7×6 容器，锁格陷阱），头号用例 W4 右缘竖缝钥匙 seed=993388590 在 3s 标准档即现「SA 无 LNS 僵局（items=10/11，total=23658）→ SA 有 LNS 全装（items=11/11，total=21144）」；预算档扫描另产出 30+ winner 种子。僵局本质：SA 贪心分数被「错放 I4v/mono 的高分局部最优」锁死（僵局态 total 反高于全装态），需多件联动才可修复。该报告的「LNS 解僵局」归因后被复验修正（见 REVERIFY）。
- **ACCEPTANCE-REPORT（首轮验收 T2~T5）**：重大发现——ewLnsSearch 两处缺陷致生产恒 no-op：①循环入口 bug（sp=0 首轮 node=-1 立即 break，根初始化与全部 DFS/UB 不可达，477/477 次调用实证）；②下推占位 bug（新节点 stackCnt=0 被弹栈循环误判候选耗尽，DFS 无法下探 depth≥1）；另 ewLnsUB 配对松弛低估（Σ单最大邻居配对 < 真实多邻居配对加成，合成对账 24/55 违规，max 低估 3408，违反「宁松不紧」）。首轮 T3 delta 对账 PASS（max|Δ|≤3.6e-12）、T4 种子复现 PASS；T2/T5 因缺陷 FAIL。如实披露：QA 清单 A/B 原始 id 未随仓库封存，按口径重建（绝对分与期 1 封存基线不可比）。
- **REVERIFY-REPORT（Terry 独立复验，不采信自测结论）**：修复后 LNS 真实执行（独立探针：node≥0 访问合计 76572、maxDepth 4~7、ok=true 合计 295，缺陷态对照全 0）；UB 双轨 0 违规（合成 55 可验证样本 minSlack=+2818；活体 831 次自然搜索 0 违规）；T4 复现 12 次运行逐字一致；N1/N2 几何不可装 sanity 32/32 无漂移；T5 G4/G5/G6 PASS、G1/G2 FAIL（预算挤占+噪声，16/16 种子保持完整打包）、G3 SKIP（harness vm 沙箱口径不可信，留待浏览器 E2E）；三档对照功效归因 FAIL（见 4.5）。

### 4.3 T2/T3/T4 PASS 证据数值（修复后复验口径）

| 项 | 证据 |
|---|---|
| T2 UB 保守性 | 合成拆除态对账：27 态/78 样本/55 可验证，**0 违规**，全局 minSlack=+2818（修复前 24/55 违规、minSlack=−3408）；活体对账：W1/W3/W4/W6 共 831 次自然 LNS 搜索 0 违规（minSlack 5844~25934） |
| T3 delta 对账 | W4（lns=false）/W1（lns=true）各 2000 步随机游走：增量自洽分量 max|Δ|≤3.6e-12 ≪ 1e-6；能量双口径对账通过（orchestrator 侧绝对差 28 为 1e17 量级双精度 ulp，相对差 4e-16） |
| T4 种子复现 | W1/W3/W4 × ±LNS × 各 2 次：done/incumbent 全字段逐字一致（12 次运行 0 不一致，含僵局态；虚拟确定性时钟口径，真实挂钟下 anytime 截断点可漂移） |

### 4.4 T5 口径与结论

- 口径披露：QA 清单按口径重建（A=12件/2手动优先级/面积33；B=16件/1手动优先级/面积41），重建清单 DFS 基准（A=14189~14227，B=31394~31476）与封存基线量级不同，绝对分不可比；判定改为 G1 SA 全胜 DFS + G2 SA-on 不低于 SA-off。
- G1 FAIL：SA-off 12/16、SA-on 11/16，失手种子差距 ≤2%（3s SA vs 20s DFS 预算不对称 + 随机波动）。
- G2 FAIL：均分 A −0.36% / B −1.09%（最差种子 −4.56%），但 16/16 种子 ±LNS 全部保持完整打包——退化是 LNS 真实执行每窗口最多 500 虚拟 ms 挤占 SA 主迭代预算的全装下分数波动，非实现缺陷。
- G4 PASS（init P95 ≤5ms：A 1.988ms / B 3.055ms）、G5 PASS（ΣbonusEvents.bonus−bonusScore 最大差=0）、G6 PASS（progress 补 fullPackingFound 后 legacy⊆SA 零缺失，新增键均加法）。
- G3 SKIP：harness vm 同步沙箱真实挂钟仅 ~6000 iters/s，无法复现期 1 浏览器基线 ≥19.4万/s，留待期 3 浏览器 E2E。
- **SA-on 关闭默认**即本轮收口的直接落地：默认档（LNS 关）行为与 5cdda6b 实验档 A 基线逐字一致（见 4.6 自测）。

### 4.5 三档对照核心数据（224 次：7 实例 × 2 预算档 × 16 种子）

三档：A=LNS 关（基线，op7/op8 短路不耗 RNG）；B=LNS 开（正式修复代码）；C=RNG 扰动对照桩（真实跑满 ewLnsOp 的 destroy RNG/DFS 时钟/成败记账，但应用改进后整体回退状态，净改动归零）。

- **winners（B 独占救回）=7 ≯ noise（C 独占救回）=28**，方向相反；凡 B 救回的种子 C 必同步救回——修复的状态贡献在 224 次运行中**零次独立显现**。
- 全局均分 **A=20853.03 / B=20718.97 / C=20878.17**：B 三档最低（低于 A **−0.64%**、低于 C −0.76%），预算挤抓实证；两两严格比较 B>C 仅 12 次 vs C>B 58 次、A>B 114 次。
- 头号用例 W4 seed=993388590 @3s：A 僵局（complete=false，items=10，total=23658，nodes 耗满 29761）；**B/C 均救回全装且结果完全相同**（items=11，total=21144）——C 未保留任何修复状态改动却同样救回，证明救回不可归因于修复搜索的状态贡献；但 **B nodes=10679 ≪ C nodes=26816**，修复后 LNS 节点效率真实更高（对最终解无差异化贡献）。600ms 低预算档三档均僵局，修复无可见功效。
- 判定：按「winners 须显著多于 noise」判定式，LNS 修复的僵局救回功效无法与 RNG+虚拟时钟扰动剥离。

### 4.6 用户决策记录与收口实现（2026-08-15）

- **决策**：期 2 按「实现正确、LNS 默认关闭」验收收口；LNS 代码保留待后续重启，直接推进期 3。
- **实现**（js/engine-worker.js + js/engine-orchestrator.js，solver-worker.js/ui-result.js 零改动）：
  - init meta 新增 `lnsEnabled` 键（缺省/非真值=关闭）；orchestrator 组装处默认 false，隐藏开关 `window.__ENGINE_LNS_ON__` 为真时置 true（与 `__ENGINE_TEMPERING_OFF__` 同风格，冻结契约只加法）。
  - 关闭时 ewRunOp 对 op7/op8 短路为 no-op 且**不消耗 RNG**；op7/op8 权重保持 0.6/地板 0.3 不清零（档 A 基线即此语义；清零会改变 ewPickOp 的 RNG→算子映射破坏逐字一致，短路先于权重生效、地板无法复活 LNS）。
  - legacy/fast 档不受影响（meta 仅发 SA Worker）。
- **自测证据**（收口前 harness 抓取基线，收口后复测）：
  - 默认关闭档 vs 改动前 5cdda6b 实验「LNS 关」档 A（W4 seed=993388590，SA 3s 虚拟时钟）：done.sol/nodes/parts **逐字一致**（nodes=29761，complete=false，items=10，total=23658）；
  - 开启档（meta.lnsEnabled=true）：LNS 真实执行，复现 REVERIFY T4 数值逐字一致（complete=true，items=11，total=21144，nodes=10679）。

### 4.7 遗留建议（期 3 重启 LNS 前必读）

1. **预算保护**：触发配额化（每 N 次 SA 迭代或停滞触发才调用）+ LNS 消耗计入总预算上限，hardMs 从 500 降档；否则均分退化（−0.64%）必复现。
2. **accept/apply 根因分析**：修复搜索的状态贡献 224 次运行零次独立显现（与纯扰动等价），需先查清 ewLnsSearch/ewLnsOp 的 accept/apply 路径为何无差异化贡献，再谈重启功效。
3. 定位从「僵局救回器」调整为「预算受控的精化器」，并以真实浏览器 E2E 复核迭代速率与功效（harness 虚拟时钟会放大 LNS 预算占比）。
4. G3 迭代速率浏览器 E2E 复验（期 3 验证项）。
5. **progress 契约（期 3 可直接依赖）**：SA progress 自 e1034dc 起恒含 fullPackingFound 键（orchestrator engOrchNoteProgress 就地补齐，只加法不删改现有键，语义 = best.complete），期 3 新增 progress 消费可直接依赖该键，无需缺失守卫。

## 四分节、期 3 对接放量实施说明（2026-08-15）

### 实施落点

1. **statusBox SA 行**：ewSendProgress 新增 `saOpTop3`（9 算子按 opWeights 排序取前 3，不耗 RNG，只加法）；solver.js updateSolverStatusFromMessage 扩键接收，renderLiveSolverStatus 条件追加 SA 行组（温度/接受率/迭代速率/重热/Top3，仅消息带 SA 键时出现）；终态在 finishWorker 调 renderStats（冻结）之后经 engOrchSaSummary 条件追加 SA 摘要段。
2. **默认 auto**：index.html selected 移至 auto + hint 同步；solver.js DOM 缺省回退改 'auto'。
3. **SAB 增强档**：engOrchSabAvailable 探测（crossOriginIsolated===true 且 SAB 可用；file:// 恒 false → broker）；回火开启且探测通过时建立 SAB 直连通道（固定槽位+序列号+Atomics 发布屏障（无 wait/notify，主循环每 2048 迭代轮询）的简单直传结构：控制区 seq/数据区 E+T0/解区，Worker 侧 ewSabExchange 就地发布+检查，读侧对解/E/T0 三检 seq 防半更新与旧解×新能量错配，接受判定 β 按台阶归属取各自标定 T0，Metropolis 判定与 broker 同式）；任何失败 try/catch 回落 broker；隐藏开关 `window.__ENGINE_SAB_OFF__` 强制 broker。SAB 档与 broker 档交换时序允许不同（跨档不要求逐字一致），K=1 不启用。
4. **docs/回归清单.md**：手工回归清单（a~j 逐项，9 张 png 基线对照）+ 20×20 扩展路径文档化。

### 关键决策：老存档兼容（拍板：保守）

页面默认档改 auto，但 persistence.js 读档缺 engineMode 字段仍回退 **legacy**（老用户读档行为不变）；已存值跟随存档。见 applySharedSettings 注释与 solver.js solveAndRender 注释。

### 铁律遵守

solver-worker.js / ui-result.js 本期零改动；legacy/fast 档消息不带 SA 键，实时/终态 SA 段条件渲染恒不出现，输出逐字不变；未引入 fetch/ESM/网络资源；RNG 基线不漂移（saOpTop3 纯上报不耗 RNG；SAB 档仅新增档内 RNG 消耗，broker 档路径逐字不变）。

## 四点五、期 3 三评审合并发现修复（2026-08-15）

### 修复落点（仅 engine-worker.js ewSabExchange/摘要链 + solver.js 渲染，broker/file://、legacy/fast 路径零漂移）

1. **W1 · SAB 接受判定消费对手 T0**：旧实现两侧台阶 β 均用本地 T0，而对手实际退火温度基于其自身标定 T0（各 Worker 因种子不同），β 量级错配使交换效率失真。修为按台阶归属取各自基温：台阶 k 属 Worker min(i,j)、台阶 k+1 属 max(i,j)，本 Worker 台阶用本地 T0、对手台阶用对手发布的 T0j（与 broker 档「用对手上报 parts.T0」语义对齐）。确定性不变：仍仅在对手有新发布时消耗一次 RNG，未新增 RNG 消耗点。
2. **W2 · 读侧三检窗口**：旧实现复核 seq 后才读 Ej，复核后对手可能再发布导致「旧解×新能量」错配。修为将 Ej/T0j 读入 seq 保护窗口：slice 解 → 读 Ej/T0j → 三检 seq（解/E/T0 同属同一次发布）→ 记 lastSeq；T0j 非法（非有限/非正）时同样放弃。
3. **S3 · 摘要文案三态**：engOrchSaSummary 新增 `tempering` 键（saWorkers.length>=2 && 实际启用）透传真实回火态；solver.js 终态摘要据 tempering/sabMode 三态渲染「SAB 直连回火通道」/「主线程 broker 回火」/「未启用回火（单 SA）」，消除 saCount=1 时谎报「主线程 broker 回火」的展示性缺陷（冻结契约只加法）。
4. **注释对齐**：SAB 通道描述统一为「固定槽位 + 序列号 + Atomics 发布屏障（无 wait/notify，主循环每 2048 迭代轮询）」；建视图失败降级注释改为「降级为 broker 交换（ewSabActive=false → 主循环回落 swap-req 路径）」。
5. **卫生**：.gitignore 追加 debug.log/*.tmp/scripts/exp/；删除临时物 scripts/exp/、.review-diff.tmp、debug.log；停止端口 8123 验证用静态服务进程；「待任务 9」表述统一为不依赖编号的「期 3 浏览器 E2E 验证」口径。

### 铁律遵守

SAB 改动仅限 ewSabExchange 与摘要链；broker 档（engOrchHandleSwapReq）、file://、legacy/fast 路径零改动；solver-worker.js、ui-result.js 零改动；新增键（tempering）均为加法。

## 四点六、期 4《我要当老祖》主题 UX 验收结论（2026-08-15）

### 实施落点（commit 3d78b06）

1. **css/styles.css 令牌化重写**：全量 CSS 变量令牌化（水墨修仙风格配色/字体/圆角/阴影令牌），组件样式一律消费令牌。
2. **index.html**：页头改双行标题（主标题+副题签），罗盘区与版式按主题调整。
3. **js/ui-library.js**：品质筛选按钮改圆徽样式并接入令牌。
4. **js/app.js**：新增主题层点击事件委托。
5. **js/ui-theme.js（新增）**：主题装饰层（氛围背景/纹理/角饰等），纯 DOM 装饰，不触碰求解与渲染契约。

### 验收证据

1. **浏览器 E2E 全项 PASS**（2026-08-15）：页头/罗盘/筛选圆徽/求解结果配色/读档恢复/statusBox SA 实时行逐项验证通过，5 张基线截图入档 `docs/e2e-t10-01-header-overview.png`、`e2e-t10-02-compass-all.png`、`e2e-t10-03-result-colored.png`、`e2e-t10-04-restored-inventory.png`、`e2e-t10-05-statusbox-sa-live.png`；该轮全页面 E2E 同时覆盖期 3 任务 30 的浏览器验证项。
2. **语法复验**：实施时系统无 node 仅做词法校验；收尾时用 Qoder RUN_AS_NODE（v24.15.0）对 js/ 全部 17 个文件逐个 `--check`，**17/17 PASS（0 FAIL）**，含重点文件 ui-theme.js、ui-library.js、app.js。

### 铁律遵守

solver-worker.js、ui-result.js 零改动；主题改造仅涉视觉层（CSS 令牌/页头版式/装饰层/筛选圆徽），未触碰消息契约、RNG 路径与输出格式。

### 遗留与后续（三条）

1. **G3 迭代速率需真实桌面浏览器复测**：期 4 E2E 在无头环境实测迭代速率 10~13.6 万/秒，**低于 15 万门禁**（无头环境限流口径，不作判定依据）；期 1 基线 19.4 万/秒（见第五节），需在真实桌面浏览器复测确认不退化。
2. **5 张《我要当老祖》游戏实机截图未随仓库封存**：当前主题以设计文档（《超越版求解引擎设计.md》）文字令牌为准；用户提供实机截图后可逐令牌比对微调。
3. **LNS 重启前置条件延续既有记录**（见 4.7）：预算配额化（触发配额+消耗计入总预算）、accept/apply 根因分析（224 次运行零独立显现）、隐藏开关 `window.__ENGINE_LNS_ON__`。

### 期 4 主题微调（8d933c1）

用户反馈三项修复——流苏挂坠统一删除（原遮挡百宝阁/天命笺标题首字）、风味大标题全部移除恢复编号式标题（1~5）、品质色联动（--quality-* 五令牌、库表/清单品质 chip、下拉色点）；评审修复五项——染色下标边界校验、圆徽 aria-pressed、盘体四角饰纹、图例按属性色对齐、推演按钮 HTML 运行期快照。浏览器复验全 PASS、控制台零错误。

## 四点七、加成聚焦求解（单属性最大化）实施说明（2026-08-16）

### 机制（输入端改写 + 输出端复算，引擎零改动）

1. **优化端**：solver.js solveAndRender 在 prepareInventoryItems() 返回后，聚焦有效时将 items[].rates 非聚焦下标置 0（stats/value 不动）；placements 与 items 共享同一 rates 数组引用，一次改写全覆盖；inventory 原始对象不受影响（stats/rates 为 prepareInventoryItems 新建数组）。legacy 的 sumRatesProduct 与 SA 的 encBuildModel 均直接消费 payload 的 stats/rates，双引擎目标函数自动收敛为「完整装入 > base+聚焦加成 > 优先级阶梯 > 邻接」，引擎文件零改动。
2. **展示端**：新装饰层 js/ui-focus.js（仿 ui-theme.js 模式）包装 renderStats：基函数执行后，聚焦会话中用 best.placements 的 uid 反查全局 inventory 取真实 baseStats/bonusRates，经 score-shared 纯函数（scoreAreAdjacent/scorePairBonusEvents，与 worker 逐字同式）复算真实全属性基础值/加成值/总分，回写 #statScore 与 #statBreakdown（聚焦行 .focus-row 高亮）并向 statusBox 追加口径说明；全程 try/catch 兜底，失败不影响冻结渲染。
3. **守卫与兼容**：聚焦且未勾「共鸣加成」时 alert 拦截；非法/空 focusAttr 回退 ''；persistence 只加法新增 focusAttr 键，老存档缺键读档回退 ''（行为不变）；app.js focusAttr change 仿 engineMode 先例清结果。

### 铁律遵守

solver-worker.js、ui-result.js 字节级零改动；score-shared/engine-*/talisman-model/state/utils/config/其余 ui-* 零改动；未聚焦时改写分支不执行、payload 不加键，逐字节不变（RNG 基线天然保护）；未引入 fetch/ESM/网络资源，file:// 兼容。

### 回退闸

`window.__FOCUS_OFF__ = true` 一键旁路聚焦（同 __ENGINE_TEMPERING_OFF__/__ENGINE_LNS_ON__ 风格）；删除 ui-focus.js 脚本登记即复原展示层。

### 回归证据（2026-08-16 验证员复验回填）

1. **RNG 基线（默认模式零回归）**：实施前基线存档 `.vercel-tmp/baseline-pre-focus.json`（snapshotA_fast / snapshotB_deep，statScore 均 14266，攻击力 319+79=398、防御 33、生命值 13835）。实施后默认模式同条件复跑：fast 档逐字一致；deep 档除坐标布局与计时/吞吐行外一致（deep 坐标差异属 SA 随机性，statNodes/属性分配检查次数为计时吞吐指标，均非 RNG 漂移）。
2. **聚焦正确性 E2E**：同档位「聚焦攻击力」复跑，真实攻击力加成 5×15.8=79 互证成立；#statBreakdown 与 statusBox 两段清单（摆放清单/百分比加成清单）口径一致（fab9a70 修复后），聚焦行唯一 .focus-row 高亮，statusBox 含「优化目标：攻击力加成最大化（忽略其它属性加成）」与聚焦口径说明。
3. **守卫**：聚焦且未勾选「共鸣加成」→ alert 拦截，文案「加成聚焦需要勾选“共鸣加成”（百分比相邻加成）。」；非法 focusAttr 值回退 ''。
4. **铁律与卫生**：js/solver-worker.js 与 js/ui-result.js 字节级零改动（git diff 校验）；js/ 全文件 node --check 18/18 通过（含新增 ui-focus.js）；file:// 控制台 0 报错；切回默认零残留。
5. **提交链**：`16d2817` feat: 加成聚焦求解（控件+逻辑+装饰层）→ `fab9a70` fix: 聚焦态清单口径修复。
6. **回退闸**：`window.__FOCUS_OFF__` 一键旁路（见上节）。

## 四点八、红品质长老星级基础属性加成实施说明（2026-08-16）

### 需求与系数

红色品质法宝新增「长老星级」1~5 可选，基础属性（攻击力/防御/生命值）按星级放大：1星+0%、2星+60%、3星+60%、4星+100%、5星+150%（系数 ×1/×1.6/×1.6/×2/×2.5，表驱动 `STAR_LEVEL_BONUS = [0, 0.6, 0.6, 1.0, 1.5]` 放 config.js，星级数/合法上界/UI 选项数由 .length 派生）；放大基础属性而非 bonusRates，value（=ΣbaseStats）与 stats 成对放大。

### 机制（源层物化，唯一放大点）

放大发生在 talisman-model.js normalizeItemRecord（inventory 记录唯一构造入口，addToInventory 与 loadConfig 均经此）：红品质且星级合法时 baseStats 逐项乘系数（`Math.round(v*f*10)/10` 消浮点噪声）且 value=Σ放大后 baseStats；新增纯函数 starMultiplier（非红/非法星级恒返 1）。全链零改动自动继承：solver.js prepared stats/value 直读 inv；legacy worker 与 SA 引擎同口径消费；冻结展示 ui-result.js 摆放清单走 stats 数组分支；聚焦装饰层 ui-focus.js 反查 inventory.baseStats 口径自动一致。

### UI 落点

法宝库品质列红品质行去品质下拉、同格新增星级下拉（hidden 双下拉切换，混合家族兜底）；清单表红法宝星级可行内编辑（normalizeItemRecord 重建记录，保留 uid/no——uid 为聚焦装饰层反查键）；存档只加法新增 starLevel 键，旧存档缺键红回退 1 星、非红 null，读档行为零变化。

### 铁律遵守与 RNG 纪律

solver-worker.js、ui-result.js 字节级零改动；solver.js/ui-focus.js/score-shared/engine-*/talisman-db/index.html/app.js/state.js 零改动。星级=1（默认）时系数 ×1 不触碰原值（整数位级一致），payload 零新键，RNG 基线天然逐字节保护。

### 回归证据（占位，待验证员回填）

- RNG 基线 diff（含红法宝清单，默认 1 星，对照 .vercel-tmp/baseline-pre-focus.json）：待回填
- 星级功能（金翎荡云 5 星 → 基础 79/13/3350 放大 197.5/32.5/8375，三处口径一致）：待回填
- UI/存档/file:// E2E 与聚焦×星级组合：待回填

## 五、期 1 验收基线（期 2 及以后不得退化）

| 指标 | 基线 |
|---|---|
| SA 3s vs DFS 20s（QA 清单口径：A=12件/2手动优先级/面积33，B=16件/1手动优先级/面积41） | 8/8 种子 cmp=1 全胜；A：68767.20（DFS 基准 66290.70，+3.7%）；B：47967~48795（基准 46021，+4.2~6.0%），全部 complete |
| 开发自测清单口径（genItems） | A 15974.96 量级、B ≈14009.64 量级 |
| init 构建（encBuildModel+encBuildBundle，P≈516） | P50=3.0ms / P95=3.9ms（≤5ms） |
| 迭代速率 | ≥19.4 万/秒（门禁 15 万） |
| 回火开销 | <2%（实测噪声范围内） |
| 明细对账 | ΣbonusEvents.bonus === bonusScore，差=0 |

## 六、换设备接续指引

1. 本仓库**无远程仓库**：换设备需整个文件夹（含 .git）拷贝，或先加 remote 推送后 clone。
2. 打开项目后对助手说：「继续超越版求解引擎任务，先读 docs/进度说明.md 与 docs/超越版求解引擎设计.md」即可从第四节断点接续。
3. 用户指令（2026-08-14）：任务 27 完成后暂停，验收等换设备后再继续。
4. 更新（2026-08-15）：期 2 已按「实现正确、LNS 默认关闭」验收收口（见第四节）；期 3 对接放量已实施完成并完成三评审合并发现修复（见第四分节/四点五分节）；期 4《我要当老祖》主题 UX 完成并验收（浏览器 E2E 全 PASS、截图入档，见四点六分节），总体进度 100%，遗留三条（G3 桌面浏览器复测/游戏实机截图待提供/LNS 重启前置条件）。

## 七、git 提交历史（相关）

- `bb05447` fix: SA引擎期1验收缺陷修复（收敛/重建守卫/init提速/消息分片）— 门禁 10/10
- `860da3c` feat: 超越版SA+ALNS求解引擎期0+1（集成脚手架）与法宝库两步式改造
- `5cdda6b` chore: 换设备封存——期2 LNS实现落盘 + 设计文档/进度说明入档
- 本次收口提交 `feat: 超越版引擎期2 LNS+配对感知上界（缺陷修复/默认关闭收口）`（hash `e1034dc`）：ewLnsSearch 循环入口/下推占位缺陷与 UB 配对松弛低估修复（T2/T3/T4 复验 PASS）、progress 契约补 fullPackingFound、LNS 默认关闭（meta.lnsEnabled + 隐藏开关 __ENGINE_LNS_ON__，关闭档与 5cdda6b 实验档 A 逐字一致）、期 2 验收与三档对照结论入档、scripts/exp/ 临时产物清理
- 期 2 评审修复提交 `fix: 期2评审缺陷修复（DFS根回溯/容差方向/init门控）`：e1034dc 三评审合并发现修复——C1 ewLnsSearch DFS 根件回溯协议缺陷（消费候选前统一撤本节点件旧位，根/非根统一，弹栈路径 no-op 兼容）、W1 剪枝容差方向修正（target 由 bTotal+1e-6 回退 bTotal+1e-9 与晋级门槛同口径）、S1 ewLnsPool 复用注释护栏、S2 ewInitLns 按 ewLnsEnabled 门控（关闭档免 LNS 初始化空转）、S3 本文档表述修正
- 期 3 评审修复提交 `fix: 期3评审修复（SAB对手T0消费/三检防错配/摘要三态/注释与卫生）`：期 3 三评审合并发现修复——W1 ewSabExchange 接受判定 β 按台阶归属消费对手发布 T0j、W2 读侧将 Ej/T0j 纳入 seq 三检保护窗口防「旧解×新能量」错配、S3 摘要文案三态（tempering 键透传 + solver.js 三态渲染）、SAB 通道/降级注释对齐、文档「待任务 9」口径统一、.gitignore 与临时物清理（详见四点五分节）
- 期 4 主题 UX 提交 `feat: 期4《我要当老祖》游戏主题UX（水墨修仙风格令牌化改造）`（hash `3d78b06`）：css/styles.css 全量令牌化重写、index.html 页头双行标题/罗盘版式、js/ui-library.js 圆徽筛选、js/app.js 主题层点击委托、新增 js/ui-theme.js 装饰层；solver-worker.js/ui-result.js 零改动
- 期 4 收尾提交 `chore: 期4收尾（截图入档/进度文档100%/语法复验）`：E2E 基线截图 e2e-t10-01~05 入档、本文档更新至总体进度 100%（期 4 ✅ 完成并验收、任务板 31/32 ✅、遗留三条）、js/ 全部 17 文件 Qoder RUN_AS_NODE `--check` 复验 17/17 PASS
- `8d933c1` fix: 期4主题微调（流苏遮挡/编号标题回归/品质色联动/评审修复）
- `feat: 加成聚焦求解（单属性最大化+装饰层真实分属性展示）`：聚焦 rates 置零（引擎零改动）+ js/ui-focus.js 装饰层复算真实分属性展示（见四点七分节）
